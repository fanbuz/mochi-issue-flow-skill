## Flow status moved

This carrier is not an active Flow Card authority.

- Flow ID: `{flowId}`
- Canonical status: `{canonicalStatusCommentUrl}`
- Previous card hash: `{sha256:...}`
- Migrated at: `{ISO-8601 timestamp}`
- Migration evidence: `{URL}`

Follow the canonical status link before reading, changing, or closing this flow. Keep this pointer immutable except to repair a broken canonical link.
