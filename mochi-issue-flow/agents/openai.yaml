interface:
  display_name: "Mochi Issue Flow"
  short_description: "Issue-driven delivery coordination"
  default_prompt: "使用 $mochi-issue-flow 协调这项工作：先用人话说明接下来要做什么和会不会产生实际改动；再恢复当前任务状态，并按需要处理跨仓库协作与验收。"
